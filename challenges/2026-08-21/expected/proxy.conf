upstream api     { host = edge.internal; port = 8080; retry = true; }
upstream worker  { host = edge.internal; port = 8081; retry = true; }
upstream events  { host = edge.internal; port = 8082; retry = true; }
upstream health  { host = edge.internal; port = 8083; retry = false; }
